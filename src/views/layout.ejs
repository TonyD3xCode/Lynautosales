<!DOCTYPE html>
<html lang="<%= (typeof lang === 'string' && lang) ? lang : 'es' %>">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title><%= (typeof title === 'string' && title.length ? title + ' · ' : '') %>LYN AutoSales</title>
  <meta name="description" content="LYN AutoSales: compra y venta de autos usados en Panama City, FL. Inventario verificado, precios competitivos y atención inmediata." />

  <link rel="stylesheet" href="/assets/styles.css" />
</head>
<body>
  <header class="site-header">
    <div class="container">
      <a class="brand" href="/"><span>LYN</span> AutoSales</a>

      <nav class="main-nav" aria-label="Main">
        <a href="/inventory"><%= __('common.inventory') %></a>
        <a href="/about"><%= __('common.about') %></a>
        <a href="/contact"><%= __('common.contact') %></a>
        <% if (typeof user === 'object' && user) { %>
          <a href="/admin"><%= __('common.admin') %></a>
          <a href="/auth/logout"><%= __('common.logout') %></a>
        <% } else { %>
          <a class="btn" href="/admin/login"><%= __('common.login') %></a>
        <% } %>
        <!-- selector de idioma -->
        <a href="?lang=es" aria-label="Español">ES</a>
        <a href="?lang=en" aria-label="English">EN</a>
      </nav>
    </div>
  </header>

  <main id="content" class="container">
    <%- body %>
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>© <%= new Date().getFullYear() %> LYN AutoSales</p>
    </div>
  </footer>
</body>
</html>
