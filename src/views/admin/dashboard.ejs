<%- include('partials/shell', {
  pageTitle: 'Escritorio',
  currentPath: '/admin',
  body: `
    <section class="grid dash-cards">
      <div class="card kpi">
        <h3>Vehículos</h3>
        <p class="big"><strong>${stats.vehicles}</strong></p>
        <p><a class="btn" href="/admin/vehicles">Gestionar</a></p>
      </div>
      <div class="card kpi">
        <h3>Consultas</h3>
        <p class="big"><strong>${stats.inquiries}</strong></p>
      </div>
      <div class="card kpi">
        <h3>Usuarios</h3>
        <p class="big"><strong>${stats.users}</strong></p>
      </div>
    </section>

    <section class="card" style="margin-top:16px">
      <div class="card-body">
        <h3>Accesos rápidos</h3>
        <div class="quick">
          <a class="btn" href="/admin/vehicles/new">+ Nuevo vehículo</a>
          <a class="btn" href="/admin/media">Medios</a>
          <a class="btn" href="/admin/settings">Ajustes del sitio</a>
        </div>
      </div>
    </section>
  `
}) %>
